<winuiex:WindowEx
    x:Class="TopToolbar.SettingsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:winuiex="using:WinUIEx"
    xmlns:local="using:TopToolbar.Models"
    xmlns:tkcontrols="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d"
    Width="1080"
    Height="720"
    MinWidth="960"
    MinHeight="640"
    Title="Toolbar Settings">

    <Grid>
        <Grid.Resources>
            <!-- Spacing tokens -->
            <x:Double x:Key="SpacingXS">4</x:Double>
            <x:Double x:Key="SpacingS">8</x:Double>
            <x:Double x:Key="SpacingM">12</x:Double>
            <x:Double x:Key="SpacingL">20</x:Double>

            <!-- Unified SettingsCard appearance inside this window -->
            <Style TargetType="tkcontrols:SettingsCard">
                <Setter Property="Margin" Value="0,0,0,12" />
                <Setter Property="CornerRadius" Value="10" />
                <Setter Property="Padding" Value="14,10" />
                <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorSecondaryBrush}" />
                <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}" />
                <Setter Property="BorderThickness" Value="1" />
            </Style>

            <!-- List item style (shared) -->
            <Style x:Key="ToolbarListItemStyle" TargetType="ListViewItem">
                <Setter Property="Margin" Value="0,4,0,0" />
                <Setter Property="Padding" Value="10,8" />
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefaultBrush}" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ListViewItem">
                            <Grid x:Name="RootGrid" Background="{TemplateBinding Background}" CornerRadius="8">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <!-- Main content presenter -->
                                <ContentPresenter
                                    x:Name="ContentPresenter"
                                    ContentTransitions="{TemplateBinding ContentTransitions}"
                                    Content="{TemplateBinding Content}"
                                    ContentTemplate="{TemplateBinding ContentTemplate}"
                                    Margin="{TemplateBinding Padding}"
                                    VerticalAlignment="Center" />
                                <!-- Overlay accent stripe -->
                                <Border x:Name="AccentStripe" Width="3" Margin="0,2,0,2" HorizontalAlignment="Left" VerticalAlignment="Stretch" CornerRadius="2" Background="Transparent" IsHitTestVisible="False" />
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal" />
                                        <VisualState x:Name="PointerOver">
                                            <VisualState.Setters>
                                                <Setter Target="RootGrid.Background" Value="{ThemeResource AccentFillColorTertiaryBrush}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Pressed">
                                            <VisualState.Setters>
                                                <Setter Target="RootGrid.Background" Value="{ThemeResource AccentFillColorSecondaryBrush}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Disabled">
                                            <VisualState.Setters>
                                                <Setter Target="RootGrid.Opacity" Value="0.5" />
                                            </VisualState.Setters>
                                        </VisualState>
                                    </VisualStateGroup>
                                    <VisualStateGroup x:Name="SelectionStates">
                                        <VisualState x:Name="Unselected" />
                                        <VisualState x:Name="Selected">
                                            <VisualState.Setters>
                                                <Setter Target="RootGrid.Background" Value="#FFF2F2F2" />
                                                <Setter Target="AccentStripe.Background" Value="{ThemeResource AccentFillColorDefaultBrush}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="SelectedPointerOver">
                                            <VisualState.Setters>
                                                <Setter Target="RootGrid.Background" Value="#FFF2F2F2" />
                                                <Setter Target="AccentStripe.Background" Value="{ThemeResource AccentFillColorDefaultBrush}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="SelectedPressed">
                                            <VisualState.Setters>
                                                <Setter Target="RootGrid.Background" Value="#FFE5E5E5" />
                                                <Setter Target="AccentStripe.Background" Value="{ThemeResource AccentFillColorDefaultBrush}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="SelectedUnfocused">
                                            <VisualState.Setters>
                                                <Setter Target="RootGrid.Background" Value="#FFF2F2F2" />
                                                <Setter Target="AccentStripe.Background" Value="{ThemeResource AccentFillColorDefaultBrush}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="SelectedDisabled">
                                            <VisualState.Setters>
                                                <Setter Target="RootGrid.Background" Value="#FFF2F2F2" />
                                                <Setter Target="AccentStripe.Background" Value="{ThemeResource AccentFillColorTertiaryBrush}" />
                                                <Setter Target="RootGrid.Opacity" Value="0.5" />
                                            </VisualState.Setters>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Title bar (drag region) redesigned: compact, no Save button -->
        <Grid x:Name="AppTitleBar" Height="44" Margin="4,4,4,0" Background="Transparent">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Spacing="10" Margin="14,0,0,0">
                <Border Width="32" Height="32" CornerRadius="8" Background="{ThemeResource LayerOnAcrylicFillColorDefaultBrush}" VerticalAlignment="Center">
                    <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE713;" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <StackPanel Spacing="2" VerticalAlignment="Center">
                    <TextBlock Text="Toolbar Settings" FontSize="16" FontWeight="SemiBold" />
                    <TextBlock Text="Configuration" FontSize="12" Opacity="0.65" />
                </StackPanel>
            </StackPanel>
        </Grid>

        <!-- Main two‑column layout -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="LeftPaneColumn" Width="300" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Left pane: group list + group properties cards -->
            <ScrollViewer Grid.Column="0" HorizontalScrollMode="Disabled" VerticalScrollBarVisibility="Auto" Padding="0">
                <Border Background="{ThemeResource LayerOnAcrylicFillColorDefaultBrush}" Padding="18,16,16,24" CornerRadius="14" Margin="8,8,0,12">
                    <StackPanel Spacing="14">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Spacing="10">
                            <TextBlock Text="Groups" FontSize="15" FontWeight="SemiBold" />
                            <Rectangle Width="1" Height="18" Fill="{ThemeResource CardStrokeColorDefaultBrush}" Opacity="0.35" />
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <Button Content="Add" Click="OnAddGroup" />
                                <Button Content="Remove" Click="OnRemoveSelectedGroup" IsEnabled="{x:Bind ViewModel.HasSelectedGroup, Mode=OneWay}" />
                            </StackPanel>
                        </StackPanel>
                        <ListView x:Name="GroupsList"
                                  ItemsSource="{x:Bind ViewModel.Groups, Mode=OneWay}"
                                  SelectedItem="{x:Bind ViewModel.SelectedGroup, Mode=TwoWay}"
                                  SelectionMode="Single"
                                  BorderThickness="0"
                                  IsItemClickEnabled="True"
                                  MinHeight="120"
                                  ItemContainerStyle="{StaticResource ToolbarListItemStyle}">
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="local:ButtonGroup">
                                    <Grid ColumnSpacing="8" Padding="8,0,8,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{x:Bind Name}" FontWeight="SemiBold" />
                                        <ToggleSwitch Grid.Column="1" IsOn="{x:Bind IsEnabled, Mode=TwoWay}" MinWidth="0" HorizontalAlignment="Right" />
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                            <ListView.Resources>
                                <Style TargetType="ListViewItem" BasedOn="{StaticResource ToolbarListItemStyle}">
                                    <Setter Property="Padding" Value="4,6" />
                                </Style>
                            </ListView.Resources>
                        </ListView>
                        <!-- Group property cards -->
                        <StackPanel Spacing="10" Visibility="{x:Bind ViewModel.HasSelectedGroup, Mode=OneWay}" Margin="0,6,0,0">
                            <tkcontrols:SettingsCard Header="Group Name">
                                <TextBox Text="{x:Bind ViewModel.SelectedGroup.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            </tkcontrols:SettingsCard>
                            <tkcontrols:SettingsCard Header="Enabled">
                                <ToggleSwitch IsOn="{x:Bind ViewModel.SelectedGroup.IsEnabled, Mode=TwoWay}" />
                            </tkcontrols:SettingsCard>
                            <tkcontrols:SettingsCard Header="Description">
                                <TextBox Text="{x:Bind ViewModel.SelectedGroup.Description, Mode=TwoWay}" AcceptsReturn="True" TextWrapping="Wrap" Height="80" />
                            </tkcontrols:SettingsCard>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </ScrollViewer>

            <!-- Divider line -->
            <Border Grid.Column="1" Background="{ThemeResource CardStrokeColorDefaultBrush}" Opacity="0.4" />

            <!-- Right pane: buttons + button details -->
            <ScrollViewer Grid.Column="2" Padding="0" VerticalScrollBarVisibility="Auto">
                <Border Background="{ThemeResource LayerOnAcrylicFillColorDefaultBrush}" CornerRadius="14" Padding="30,20,30,40" Margin="0,8,8,12">
                    <StackPanel Spacing="26">
                        <StackPanel Orientation="Horizontal" Spacing="10" Visibility="{x:Bind ViewModel.HasSelectedGroup, Mode=OneWay}" VerticalAlignment="Center">
                            <TextBlock Text="Buttons" FontSize="15" FontWeight="SemiBold" />
                            <Rectangle Width="1" Height="18" Fill="{ThemeResource CardStrokeColorDefaultBrush}" Opacity="0.35" />
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <Button Content="Add" Click="OnAddButton" Tag="{x:Bind ViewModel.SelectedGroup}" />
                                <Button Content="Remove" Click="OnRemoveButton" Tag="{x:Bind ViewModel.SelectedButton}" IsEnabled="{x:Bind ViewModel.HasSelectedButton, Mode=OneWay}" />
                            </StackPanel>
                        </StackPanel>

                        <ListView x:Name="ButtonsList"
                                  ItemsSource="{x:Bind ViewModel.SelectedGroup.Buttons, Mode=OneWay}"
                                  SelectedItem="{x:Bind ViewModel.SelectedButton, Mode=TwoWay}"
                                  SelectionMode="Single"
                                  BorderThickness="0"
                                  Visibility="{x:Bind ViewModel.HasSelectedGroup, Mode=OneWay}"
                                  MinHeight="120"
                                  ItemContainerStyle="{StaticResource ToolbarListItemStyle}">
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="local:ToolbarButton">
                                    <Grid ColumnSpacing="8" Padding="8,0,8,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <ToggleSwitch Grid.Column="0" IsOn="{x:Bind IsEnabled, Mode=TwoWay}" />
                                        <TextBlock Grid.Column="1" Text="{x:Bind Name}" VerticalAlignment="Center" />
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>

                    <!-- Button detail cards -->
                    <StackPanel Spacing="12" Visibility="{x:Bind ViewModel.HasSelectedButton, Mode=OneWay}">
                        <tkcontrols:SettingsCard Header="Button Name">
                            <TextBox Text="{x:Bind ViewModel.SelectedButton.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        </tkcontrols:SettingsCard>
                        <tkcontrols:SettingsCard Header="Description">
                            <TextBox Text="{x:Bind ViewModel.SelectedButton.Description, Mode=TwoWay}" />
                        </tkcontrols:SettingsCard>
                        <tkcontrols:SettingsCard Header="Icon Type">
                            <ComboBox SelectedIndex="{x:Bind ViewModel.SelectedButton.IconTypeIndex, Mode=TwoWay}">
                                <ComboBoxItem Content="Glyph" />
                                <ComboBoxItem Content="Image" />
                            </ComboBox>
                        </tkcontrols:SettingsCard>
                        <tkcontrols:SettingsCard Header="Glyph">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="48" />
                                </Grid.ColumnDefinitions>
                                <TextBox Grid.Column="0" Text="{x:Bind ViewModel.SelectedButton.IconGlyph, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                <Border Grid.Column="1" Background="{ThemeResource SubtleFillColorSecondaryBrush}" CornerRadius="4" Margin="8,0,0,0">
                                    <TextBlock Text="{x:Bind ViewModel.SelectedButton.IconGlyph}" FontFamily="Segoe MDL2 Assets" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </Border>
                            </Grid>
                        </tkcontrols:SettingsCard>
                        <tkcontrols:SettingsCard Header="Image Path">
                            <Grid ColumnSpacing="8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBox Grid.Column="0" Text="{x:Bind ViewModel.SelectedButton.IconPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                <Button Grid.Column="1" Content="Browse" Click="OnBrowseIcon" Tag="{x:Bind ViewModel.SelectedButton}" />
                            </Grid>
                        </tkcontrols:SettingsCard>
                        <tkcontrols:SettingsCard Header="Command">
                            <TextBox Text="{x:Bind ViewModel.SelectedButton.Action.Command, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        </tkcontrols:SettingsCard>
                        <tkcontrols:SettingsCard Header="Arguments">
                            <TextBox Text="{x:Bind ViewModel.SelectedButton.Action.Arguments, Mode=TwoWay}" />
                        </tkcontrols:SettingsCard>
                        <tkcontrols:SettingsCard Header="Working Directory">
                            <TextBox Text="{x:Bind ViewModel.SelectedButton.Action.WorkingDirectory, Mode=TwoWay}" />
                        </tkcontrols:SettingsCard>
                    </StackPanel>

                    <StackPanel Visibility="{x:Bind ViewModel.HasNoSelectedGroup, Mode=OneWay}" HorizontalAlignment="Center" Margin="0,60,0,0" Spacing="12">
                        <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE713;" FontSize="42" Opacity="0.25" />
                        <TextBlock Text="Select a group to configure" Opacity="0.6" />
                    </StackPanel>
                </StackPanel>
                </Border>
            </ScrollViewer>
        </Grid>
    </Grid>
</winuiex:WindowEx>